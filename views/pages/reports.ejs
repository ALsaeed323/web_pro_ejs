<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Reports</title>
  <style>
    table {
      border-collapse: collapse;
      width: 100%;
    }

    th,
    td {
      padding: 8px;
      text-align: center;
      border-bottom: 1px solid #ddd;
    }

    th {
      background-color: #f2f2f2;
    }

    .generate-Reports {
      margin-top: 20px;
    }

    .add-button {
      margin-top: 20px;
      background-color: red;
      color: white;
      padding: 10px 20px;
      border: none;
      cursor: pointer;
      border-radius: 4px;
    }

    .add-button:hover {
      background-color: #c70000;
    }

    .submit {
      margin-top: 20px;
      background-color: #4CAF50;
      color: white;
      padding: 10px 20px;
      border: none;
      cursor: pointer;
      border-radius: 4px;
    }

    .submit:hover {
      background-color: #45a049;
    }
  </style>
</head>

<body>
  <h2>Reports</h2>
  <table id="reportTable">
    <thead>
      <tr>
        <th>what are the issues you had when you were using our website ?</th>
      </tr>
    </thead>
    <tbody class="body">
      <tr>
        <td><input type='text'></td>
      </tr>
    </tbody>
  </table>
  <button class="add-button" onclick="addRow()">Add Row</button>
  <button class="submit" onclick="submitReport()">Submit</button>

  <script>
    function addRow() {
      var table = document.getElementById("reportTable").getElementsByTagName('tbody')[0];
      var newRow = table.insertRow(table.rows.length);
      var columnsCount = table.rows[0].cells.length;

      for (var i = 0; i < columnsCount; i++) {
        var cell = newRow.insertCell(i);
        cell.innerHTML = "<input type='text'>";
      }
    }

    function submitReport() {
      var table = document.getElementById("reportTable");
      var data = [];
      for (var i = 1; i < table.rows.length; i++) {
        var rowData = [];
        for (var j = 0; j < table.rows[i].cells.length; j++) {
          var cellData = table.rows[i].cells[j].querySelector('input').value;
          rowData.push(cellData);
        }

        data.push(rowData);
      }

      
      console.log("Submitting the following data:", data);
      var tbody = document.querySelector("#reportTable tbody");
      tbody.innerHTML = "";
      var firstRowInputs = document.querySelectorAll("#reportTable tbody tr:first-child input");
      firstRowInputs.forEach(function (input) {
        input.value = "";
      });
    }
  </script>
</body>

</html>